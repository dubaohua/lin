<%= form_for(topic1) do |f| %>
  <% if topic1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(topic1.errors.count, "error") %> prohibited this topic1 from being saved:</h2>

      <ul>
      <% topic1.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
